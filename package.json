{
  "name": "@calvinusesyourcode/types",
  "license": "MIT",
  "version": "2024.6.10",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup index.ts --format cjs,esm --dts",
    "push": "git add . && git commit -m ^ && git push",
    "lint": "tsc",
    "publish": "npm publish --access=public",
    "deploy": "npm run build && npm run lint && npm run push && npm run publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.5",
    "tsup": "^8.1.0",
    "typescript": "^5.4.5"
  }
}